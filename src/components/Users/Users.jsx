import React from 'react';

const Users = (props) =>
{
    props.users.map(u=>{
        <div>
            
        </div>
    });
    
    return(
        <div>Users</div>
    );
}

export default Users;